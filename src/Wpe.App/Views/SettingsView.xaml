<UserControl x:Class="Wpe.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="{StaticResource Font.UI}">

    <UserControl.Resources>
        <Style x:Key="FieldLabel" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource Brush.TextSecondary}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,14,0,5" />
        </Style>
        <Style x:Key="SectionTitle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource Brush.TextPrimary}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding Title}" Style="{StaticResource Text.Title}" />
            <TextBlock Text="{Binding Description}" Style="{StaticResource Text.Subtle}" Margin="0,6,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,18,0,0">
            <StackPanel MaxWidth="640" HorizontalAlignment="Left">

                <!-- 通用 -->
                <Border Style="{StaticResource Card}" Effect="{StaticResource Shadow.Card}">
                    <StackPanel>
                        <TextBlock Text="通用" Style="{StaticResource SectionTitle}" />
                        <TextBlock Text="界面语言" Style="{StaticResource FieldLabel}" />
                        <ComboBox ItemsSource="{Binding Languages}" DisplayMemberPath="Name"
                                  SelectedValuePath="Code" SelectedValue="{Binding SelectedLanguage}"
                                  Width="220" HorizontalAlignment="Left" />
                        <CheckBox Content="显示调试日志（ShowDebug）" IsChecked="{Binding ShowDebug}" Margin="0,18,0,0"
                                  Foreground="{StaticResource Brush.TextSecondary}" />
                    </StackPanel>
                </Border>

                <!-- 远程管理 -->
                <Border Style="{StaticResource Card}" Effect="{StaticResource Shadow.Card}" Margin="0,16,0,0">
                    <StackPanel>
                        <TextBlock Text="远程管理" Style="{StaticResource SectionTitle}" />
                        <CheckBox Content="启用远程管理服务（WebAPI）" IsChecked="{Binding IsRemote}" Margin="0,12,0,0"
                                  Foreground="{StaticResource Brush.TextSecondary}" />
                        <Grid IsEnabled="{Binding IsRemote}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="服务地址 (URL)" Style="{StaticResource FieldLabel}" />
                                <TextBox Text="{Binding RemoteUrl, UpdateSourceTrigger=PropertyChanged}"
                                         FontFamily="{StaticResource Font.Mono}" VerticalContentAlignment="Center" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="端口" Style="{StaticResource FieldLabel}" />
                                <TextBox Text="{Binding RemotePort, UpdateSourceTrigger=PropertyChanged}"
                                         FontFamily="{StaticResource Font.Mono}" VerticalContentAlignment="Center" />
                            </StackPanel>
                        </Grid>
                        <Grid IsEnabled="{Binding IsRemote}" Margin="0,0,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="用户名" Style="{StaticResource FieldLabel}" />
                                <TextBox Text="{Binding RemoteUserName, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="密码" Style="{StaticResource FieldLabel}" />
                                <TextBox Text="{Binding RemotePassWord, UpdateSourceTrigger=PropertyChanged}"
                                         FontFamily="{StaticResource Font.Mono}" VerticalContentAlignment="Center" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 安全 -->
                <Border Style="{StaticResource Card}" Effect="{StaticResource Shadow.Card}" Margin="0,16,0,24">
                    <StackPanel>
                        <TextBlock Text="安全" Style="{StaticResource SectionTitle}" />
                        <TextBlock Text="数据加密密钥 (AES Key)" Style="{StaticResource FieldLabel}" />
                        <TextBox Text="{Binding AESKey, UpdateSourceTrigger=PropertyChanged}"
                                 FontFamily="{StaticResource Font.Mono}" VerticalContentAlignment="Center" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
